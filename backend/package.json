{
  "name": "picme-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "render:build": "cd ../frontend && npm install && npm run build && cp -r build ../backend",
    "render:start": "npm install && npm run start"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  }
}
